apiVersion: v1
kind: Namespace
metadata:
  name: solution-based-monitoring-fail-do
  labels:
    syn_team: nunki
    vshn_net_solution_monitoring: "test-solution-base-monitoring"
---
apiVersion: batch/v1
kind: Job
metadata:
  name: failing-job
  namespace: solution-based-monitoring-fail-do
spec:
  parallelism: 1
  completions: 1
  backoffLimit: 0
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: failing-container
          image: busybox
          command: ["sh", "-c", "sleep infinity"]
